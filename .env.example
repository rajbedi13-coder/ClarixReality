# API Server (set these in Railway)
DATABASE_URL=            # auto-provided by Railway Postgres addon
PORT=                    # auto-provided by Railway
TOKEN_SECRET=            # random 32+ char string, e.g. openssl rand -hex 32
OPENAI_API_KEY=          # for news ingestion pipeline
NODE_ENV=production
